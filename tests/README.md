# Tests

This folder holds the unit tests for the repo.

## Files
- `test_security.py` - checks policy validation, trust scoring, state changes, and admin-route protection.

## What It Does
These tests make sure the rules that protect devices and API routes keep working as the code changes.
